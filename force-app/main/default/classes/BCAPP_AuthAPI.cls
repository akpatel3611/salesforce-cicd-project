// Auto-generated by bloom_bootstrap.py — edit handler classes for business logic
@RestResource(urlMapping='/v1/auth/*')
global with sharing class BCAPP_AuthAPI {

    @HttpPost
    global static void doPost() {
        RestRequest req = RestContext.request;
        Map<String,Object> body = (Map<String,Object>) JSON.deserializeUntyped(req.requestBody.toString());
        String seg = req.requestURI.substringAfterLast('/');
        switch on seg {
            when 'create-user'    { BCAPP_AuthHandler.createAzureUser(body); }
            when 'deactivate-user'{ BCAPP_AuthHandler.deactivateAzureUser(body); }
            when 'send-otp'       { BCAPP_AuthHandler.sendOTP(body); }
            when 'validate-otp'   { BCAPP_AuthHandler.validateOTP(body); }
            when 'forgot-password'{ BCAPP_AuthHandler.forgotPassword(body); }
            when 'login-history'  { BCAPP_AuthHandler.createLoginHistory(body); }
            when else             { BCAPP_ResponseHelper.error('Unknown auth operation: ' + seg); }
        }
    }
}