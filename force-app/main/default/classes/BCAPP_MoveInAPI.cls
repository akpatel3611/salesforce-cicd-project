// Auto-generated by bloom_bootstrap.py — edit handler classes for business logic
@RestResource(urlMapping='/v1/move-in/*')
global with sharing class BCAPP_MoveInAPI {

    @HttpGet
    global static void doGet() {
        RestRequest req = RestContext.request;
        String seg = req.requestURI.substringAfterLast('/');
        switch on seg {
            when 'requests'    { BCAPP_MoveInHandler.getMIPRequests(req); }
            when 'status'      { BCAPP_MoveInHandler.getMIPStatus(req); }
            when else          { BCAPP_ResponseHelper.error('Unknown endpoint: ' + seg); }
        }
    }

    @HttpPost
    global static void doPost() {
        RestRequest req = RestContext.request;
        Map<String,Object> body = (Map<String,Object>) JSON.deserializeUntyped(req.requestBody.toString());
        String seg = req.requestURI.substringAfterLast('/');
        switch on seg {
            when 'create'            { BCAPP_MoveInHandler.createMIPRequest(body); }
            when 'create-owner'      { BCAPP_MoveInHandler.createOwnerMIPRequest(body); }
            when 'resubmit'          { BCAPP_MoveInHandler.resubmitRFI(body); }
            when 'validate'          { BCAPP_MoveInHandler.validateBusinessRules(body); }
            when 'download-pdf'      { BCAPP_MoveInHandler.getMIPPdfUrl(body); }
            when 'docusign-callback' { BCAPP_MoveInHandler.handleDocuSignCallback(body); }
            when else                { BCAPP_ResponseHelper.error('Unknown operation: ' + seg); }
        }
    }
}