// Auto-generated by bloom_bootstrap.py — edit handler classes for business logic
@IsTest
private class BCAPP_AllAPI_Test {

    @TestSetup
    static void setupData() {
        // Community test record
        Community__c comm = new Community__c(
            Community_Name__c = 'Test Community',
            Country__c        = 'UAE',
            Is_Active__c      = true,
            Payment_Gateway__c= 'CyberSource',
            Document_Naming__c= 'EJARI'
        );
        insert comm;
    }

    // ── Common API Tests ──────────────────────────────────
    @IsTest static void test_CommonAPI_getProjects() {
        RestRequest req   = new RestRequest();
        RestResponse res  = new RestResponse();
        req.requestURI    = '/services/apexrest/v1/common/projects';
        req.httpMethod    = 'GET';
        RestContext.request  = req;
        RestContext.response = res;
        Test.startTest();
        BCAPP_CommonAPI.doGet();
        Test.stopTest();
        System.assert(res.statusCode == 200 || res.statusCode == null);
    }

    @IsTest static void test_CommonAPI_getBuildings() {
        RestRequest req  = new RestRequest();
        RestResponse res = new RestResponse();
        req.requestURI   = '/services/apexrest/v1/common/buildings';
        req.httpMethod   = 'POST';
        req.requestBody  = Blob.valueOf(JSON.serialize(new Map<String,Object>{ 'projectId' => 'TEST' }));
        RestContext.request  = req;
        RestContext.response = res;
        Test.startTest();
        BCAPP_CommonAPI.doPost();
        Test.stopTest();
        System.assert(res.statusCode == 200 || res.statusCode == null);
    }

    // ── Auth API Tests ────────────────────────────────────
    @IsTest static void test_AuthAPI_sendOTP() {
        RestRequest req  = new RestRequest();
        RestResponse res = new RestResponse();
        req.requestURI   = '/services/apexrest/v1/auth/send-otp';
        req.httpMethod   = 'POST';
        req.requestBody  = Blob.valueOf(JSON.serialize(new Map<String,Object>{
            'email' => 'test@test.com', 'type' => 'email'
        }));
        RestContext.request  = req;
        RestContext.response = res;
        Test.startTest();
        BCAPP_AuthAPI.doPost();
        Test.stopTest();
        System.assert(res.statusCode == 200 || res.statusCode == null);
    }

    // ── Move In API Tests ─────────────────────────────────
    @IsTest static void test_MoveInAPI_validate() {
        RestRequest req  = new RestRequest();
        RestResponse res = new RestResponse();
        req.requestURI   = '/services/apexrest/v1/move-in/validate';
        req.httpMethod   = 'POST';
        req.requestBody  = Blob.valueOf(JSON.serialize(new Map<String,Object>{
            'unitId' => 'TEST001', 'communityId' => 'TEST'
        }));
        RestContext.request  = req;
        RestContext.response = res;
        Test.startTest();
        BCAPP_MoveInAPI.doPost();
        Test.stopTest();
        System.assert(res.statusCode == 200 || res.statusCode == null);
    }

    @IsTest static void test_MoveInAPI_getRequests() {
        RestRequest req  = new RestRequest();
        RestResponse res = new RestResponse();
        req.requestURI   = '/services/apexrest/v1/move-in/requests';
        req.httpMethod   = 'GET';
        RestContext.request  = req;
        RestContext.response = res;
        Test.startTest();
        BCAPP_MoveInAPI.doGet();
        Test.stopTest();
        System.assert(res.statusCode == 200 || res.statusCode == null);
    }

    // ── Access Permit Tests ───────────────────────────────
    @IsTest static void test_AccessPermitAPI_options() {
        RestRequest req  = new RestRequest();
        RestResponse res = new RestResponse();
        req.requestURI   = '/services/apexrest/v1/access-permit/options';
        req.httpMethod   = 'GET';
        RestContext.request  = req;
        RestContext.response = res;
        Test.startTest();
        BCAPP_AccessPermitAPI.doGet();
        Test.stopTest();
        System.assert(res.statusCode == 200 || res.statusCode == null);
    }

    // ── Guest API Tests ───────────────────────────────────
    @IsTest static void test_GuestAPI_list() {
        RestRequest req  = new RestRequest();
        RestResponse res = new RestResponse();
        req.requestURI   = '/services/apexrest/v1/guest/list';
        req.httpMethod   = 'GET';
        RestContext.request  = req;
        RestContext.response = res;
        Test.startTest();
        BCAPP_GuestAPI.doGet();
        Test.stopTest();
        System.assert(res.statusCode == 200 || res.statusCode == null);
    }

    // ── Vacation API Tests ────────────────────────────────
    @IsTest static void test_VacationAPI_create() {
        RestRequest req  = new RestRequest();
        RestResponse res = new RestResponse();
        req.requestURI   = '/services/apexrest/v1/vacation/create';
        req.httpMethod   = 'POST';
        req.requestBody  = Blob.valueOf(JSON.serialize(new Map<String,Object>{
            'unitNumber' => 'B01-101', 'fromDate' => '2025-06-01', 'toDate' => '2025-06-15'
        }));
        RestContext.request  = req;
        RestContext.response = res;
        Test.startTest();
        BCAPP_VacationAPI.doPost();
        Test.stopTest();
        System.assert(res.statusCode == 200 || res.statusCode == null);
    }

    // ── Notices API Tests ─────────────────────────────────
    @IsTest static void test_NoticesAPI_getNotices() {
        RestRequest req  = new RestRequest();
        RestResponse res = new RestResponse();
        req.requestURI   = '/services/apexrest/v1/notices/notices';
        req.httpMethod   = 'GET';
        RestContext.request  = req;
        RestContext.response = res;
        Test.startTest();
        BCAPP_NoticesAPI.doGet();
        Test.stopTest();
        System.assert(res.statusCode == 200 || res.statusCode == null);
    }

    // ── Profile API Tests ─────────────────────────────────
    @IsTest static void test_ProfileAPI_get() {
        RestRequest req  = new RestRequest();
        RestResponse res = new RestResponse();
        req.requestURI   = '/services/apexrest/v1/profile/get';
        req.httpMethod   = 'GET';
        RestContext.request  = req;
        RestContext.response = res;
        Test.startTest();
        BCAPP_ProfileAPI.doGet();
        Test.stopTest();
        System.assert(res.statusCode == 200 || res.statusCode == null);
    }

    // ── Renewal / MoveOut Tests ───────────────────────────
    @IsTest static void test_RenewalAPI_list() {
        RestRequest req  = new RestRequest();
        RestResponse res = new RestResponse();
        req.requestURI   = '/services/apexrest/v1/renewal/list';
        req.httpMethod   = 'GET';
        RestContext.request  = req;
        RestContext.response = res;
        Test.startTest();
        BCAPP_RenewalMoveOutAPI.doGet();
        Test.stopTest();
        System.assert(res.statusCode == 200 || res.statusCode == null);
    }
}