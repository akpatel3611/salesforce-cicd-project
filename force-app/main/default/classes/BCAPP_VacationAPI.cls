// Auto-generated by bloom_bootstrap.py — edit handler classes for business logic
@RestResource(urlMapping='/v1/vacation/*')
global with sharing class BCAPP_VacationAPI {

    @HttpGet
    global static void doGet() {
        RestRequest req = RestContext.request;
        String seg = req.requestURI.substringAfterLast('/');
        switch on seg {
            when 'status' { BCAPP_VacationHandler.getVacationStatus(req); }
            when else     { BCAPP_ResponseHelper.error('Unknown endpoint: ' + seg); }
        }
    }

    @HttpPost
    global static void doPost() {
        RestRequest req = RestContext.request;
        Map<String,Object> body = (Map<String,Object>) JSON.deserializeUntyped(req.requestBody.toString());
        String seg = req.requestURI.substringAfterLast('/');
        switch on seg {
            when 'create' { BCAPP_VacationHandler.createVacation(body); }
            when 'cancel' { BCAPP_VacationHandler.cancelVacation(body); }
            when else     { BCAPP_ResponseHelper.error('Unknown operation: ' + seg); }
        }
    }
}