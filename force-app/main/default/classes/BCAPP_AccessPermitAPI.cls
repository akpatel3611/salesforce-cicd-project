// Auto-generated by bloom_bootstrap.py — edit handler classes for business logic
@RestResource(urlMapping='/v1/access-permit/*')
global with sharing class BCAPP_AccessPermitAPI {

    @HttpGet
    global static void doGet() {
        RestRequest req = RestContext.request;
        String seg = req.requestURI.substringAfterLast('/');
        switch on seg {
            when 'options'   { BCAPP_AccessPermitHandler.getPermitOptions(req); }
            when 'list'      { BCAPP_AccessPermitHandler.getPermitList(req); }
            when else        { BCAPP_ResponseHelper.error('Unknown endpoint: ' + seg); }
        }
    }

    @HttpPost
    global static void doPost() {
        RestRequest req = RestContext.request;
        Map<String,Object> body = (Map<String,Object>) JSON.deserializeUntyped(req.requestBody.toString());
        String seg = req.requestURI.substringAfterLast('/');
        switch on seg {
            when 'create'    { BCAPP_AccessPermitHandler.createPermit(body); }
            when 'cancel'    { BCAPP_AccessPermitHandler.cancelPermit(body); }
            when 'checkin'   { BCAPP_AccessPermitHandler.securityCheckIn(body); }
            when 'checkout'  { BCAPP_AccessPermitHandler.securityCheckOut(body); }
            when 'pdf'       { BCAPP_AccessPermitHandler.downloadPermitPDF(body); }
            when else        { BCAPP_ResponseHelper.error('Unknown operation: ' + seg); }
        }
    }
}