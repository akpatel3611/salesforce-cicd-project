// Auto-generated by bloom_bootstrap.py — edit handler classes for business logic
@RestResource(urlMapping='/v1/profile/*')
global with sharing class BCAPP_ProfileAPI {

    @HttpGet
    global static void doGet() {
        RestRequest req = RestContext.request;
        String seg = req.requestURI.substringAfterLast('/');
        switch on seg {
            when 'get'         { BCAPP_ProfileHandler.getProfile(req); }
            when 'portfolio'   { BCAPP_ProfileHandler.getPortfolio(req); }
            when 'documents'   { BCAPP_ProfileHandler.getDocuments(req); }
            when 'favourites'  { BCAPP_ProfileHandler.getFavourites(req); }
            when else          { BCAPP_ResponseHelper.error('Unknown endpoint: ' + seg); }
        }
    }

    @HttpPost
    global static void doPost() {
        RestRequest req = RestContext.request;
        Map<String,Object> body = (Map<String,Object>) JSON.deserializeUntyped(req.requestBody.toString());
        String seg = req.requestURI.substringAfterLast('/');
        switch on seg {
            when 'update'   { BCAPP_ProfileHandler.updateProfile(body); }
            when 'send-otp' { BCAPP_ProfileHandler.sendProfileOTP(body); }
            when 'verify-otp'{ BCAPP_ProfileHandler.verifyProfileOTP(body); }
            when else        { BCAPP_ResponseHelper.error('Unknown operation: ' + seg); }
        }
    }
}