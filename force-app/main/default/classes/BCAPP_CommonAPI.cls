// Auto-generated by bloom_bootstrap.py — edit handler classes for business logic
@RestResource(urlMapping='/v1/common/*')
global with sharing class BCAPP_CommonAPI {

    @HttpGet
    global static void doGet() {
        RestRequest req = RestContext.request;
        String seg = req.requestURI.substringAfterLast('/');
        switch on seg {
            when 'projects'      { BCAPP_CommonService.getProjects(req); }
            when 'nationalities' { BCAPP_CommonService.getNationalities(); }
            when 'config'        { BCAPP_CommonService.getAppConfig(req); }
            when else            { BCAPP_ResponseHelper.error('Unknown endpoint: ' + seg); }
        }
    }

    @HttpPost
    global static void doPost() {
        RestRequest req = RestContext.request;
        Map<String,Object> body = (Map<String,Object>) JSON.deserializeUntyped(req.requestBody.toString());
        String seg = req.requestURI.substringAfterLast('/');
        switch on seg {
            when 'buildings'        { BCAPP_CommonService.getBuildings(body); }
            when 'units'            { BCAPP_CommonService.getUnits(body); }
            when 'upload-doc'       { BCAPP_CommonService.uploadDocument(body); }
            when 'service-catalogs' { BCAPP_CommonService.getServiceCatalogs(); }
            when 'feedback'         { BCAPP_CommonService.submitFeedback(body); }
            when else               { BCAPP_ResponseHelper.error('Unknown endpoint: ' + seg); }
        }
    }
}